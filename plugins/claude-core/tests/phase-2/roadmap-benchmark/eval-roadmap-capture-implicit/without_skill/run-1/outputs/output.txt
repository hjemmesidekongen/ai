**No, this should not go to the roadmap.** The capability already exists.

The `storybook-generator` skill (`plugins/agency/skills/dev/storybook-generator/SKILL.md`) already does exactly this — it auto-generates Storybook story templates from component-specs output. It reads component definitions (props, slots, states, a11y requirements), combines them with page copy and UX microcopy, and produces per-component `.stories.yml` files covering all variants and viewports.

It's phase 6 in the agency dev pipeline, sits downstream of `component-specs`, and was built in Step 82. The brainstorm decisions also explicitly called this out (from the ecosystem-restructure session): *"Must output machine-readable component specs so dev can auto-generate Storybook stories."*

Adding a roadmap item for something already designed and built would be noise. If anything, the user discovering this organically is a signal the pipeline design is sound — they're arriving at the same conclusion independently.

The right move: stay focused on component specs (as they said), and when the time comes, point them to the existing storybook-generator skill.