# MCP servers relevant to the TypeScript framework pack
# Agents load this to know which MCPs to call for this domain

relevant_mcps:
  - name: "eslint"
    purpose: "Code quality and linting for JavaScript/TypeScript projects with TypeScript-specific rules"
    when_to_use: "When enforcing TypeScript typing rules, catching type errors, improving code quality, or reviewing type safety standards"
