---
name: prisma-patterns
description: >
  Prisma schema design, migrations, relations, raw queries, seeding, and
  performance optimization. Covers schema modeling decisions, migration
  workflow, query patterns, transactions, and common anti-patterns.
user_invocable: false
interactive: false
model_tier: senior
depends_on: []
triggers:
  - "prisma"
  - "prisma schema"
  - "prisma migrations"
  - "prisma relations"
reads: []
writes: []
checkpoint:
  type: data_validation
  required_checks:
    - name: "indexes_on_foreign_keys"
      verify: "Every relation field used in where/orderBy has a corresponding @@index"
      fail_action: "Add missing indexes — FK fields without indexes cause full table scans"
    - name: "no_select_star_in_loops"
      verify: "findMany calls inside loops replaced with batched include or a single query"
      fail_action: "Restructure to eliminate N+1 — use include, nested findMany, or raw SQL"
    - name: "migrations_not_hand_edited"
      verify: "Migration SQL was generated by prisma migrate dev, not hand-written"
      fail_action: "Regenerate migration or apply edits only via a new migration file"
    - name: "transactions_for_multi_write"
      verify: "Any operation writing to 2+ tables is wrapped in $transaction"
      fail_action: "Wrap multi-table writes in $transaction to prevent partial state"
  on_fail: "Prisma usage has structural problems — address before merging"
  on_pass: "Prisma patterns are sound"
_source:
  origin: "dev-engine"
  inspired_by: "original"
  ported_date: "2026-03-10"
  iteration: 1
  changes: "Original skill, no port"
---

# prisma-patterns

Model the domain first, then let Prisma reflect it. Schema decisions made carelessly early become expensive migrations later.

## Schema Design

Define relations explicitly with `@relation`. Always add `@@index` on fields used in `where`, `orderBy`, or as FK targets. Use `@@map` and `@map` to decouple Prisma names from database names — keeps the schema clean while the DB follows conventions.

Enums in Prisma map to DB enums. Prefer them for stable, bounded sets. For extensible value sets, use a string field with application-level validation instead.

## Migration Workflow

- `prisma migrate dev` — development: generates + applies + regenerates client. Use this locally.
- `prisma migrate deploy` — production: applies pending migrations only. Never generates. CI/CD safe.
- Never edit migration SQL by hand after it's been applied to any environment. Create a new migration instead.
- Baseline an existing DB with `migrate diff` + `migrate resolve --applied` before introducing Prisma to a legacy schema.

## Query Optimization

Default rule: select only what you need. Use `select` to project specific fields rather than pulling full records. Use `include` only for relations actually rendered. Avoid `findMany` inside loops — batch with `include` or a single query with `where: { id: { in: ids } }`.

Add `take` and `skip` on any unbounded `findMany`. Paginate by cursor (`cursor` + `take`) for large datasets — offset pagination degrades at scale.

## Raw Queries

Use `$queryRaw` for reads that require DB-specific syntax (window functions, CTEs, full-text search). Use `$executeRaw` for writes. Always use tagged template literals — never string interpolation — to prevent SQL injection. Raw results are not typed by default; cast with `Prisma.sql` and validate output shape.

## Transactions

Use `$transaction([...])` for sequential operations that must succeed together. Use the interactive transaction (`$transaction(async (tx) => { ... })` for conditional logic mid-transaction. Keep transactions short — long-running transactions cause lock contention.

See `references/process.md` for schema patterns, seeding, Prisma Client extensions, middleware, soft deletes, multi-tenancy, and anti-patterns with fixes.
